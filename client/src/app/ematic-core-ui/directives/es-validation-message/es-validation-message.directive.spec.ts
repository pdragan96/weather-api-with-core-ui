import { EsValidationMessageDirective } from './es-validation-message.directive';

xdescribe('EsValidationMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new EsValidationMessageDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
