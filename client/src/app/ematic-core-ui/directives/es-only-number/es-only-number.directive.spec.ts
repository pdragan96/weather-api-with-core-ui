import { EsOnlyNumberDirective } from './es-only-number.directive';

xdescribe('EsOnlyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new EsOnlyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
