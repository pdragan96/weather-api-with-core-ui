import { EsTooltipDirective } from './es-tooltip.directive';

xdescribe('EsTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new EsTooltipDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
