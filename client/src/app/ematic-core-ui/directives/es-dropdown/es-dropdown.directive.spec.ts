import { EsDropdownDirective } from './es-dropdown.directive';

xdescribe('EsDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new EsDropdownDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
